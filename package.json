{
  "name": "vector-grid",
  "version": "0.2.0",
  "private": true,
  "description": "Deterministic tactical strategy game for iOS and web.",
  "scripts": {
    "ios:prepare": "node scripts/prepare-ios.mjs",
    "ios:add": "npm run ios:prepare && npx cap add ios",
    "ios:sync": "npm run ios:prepare && npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "@capacitor/ios": "latest"
  }
}
